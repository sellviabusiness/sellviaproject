# SellVia Backend

FastAPI (Python), modular monolith. See `Docs/Technical Architecture/Backend Architecture` for the full design.

## Run locally

```
pip install -r requirements.txt
cp .env.example .env
uvicorn app.main:app --reload
```
